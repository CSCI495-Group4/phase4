# Model A (ResNet18 + GRU Fusion)

## Steps to Run
1. Load modules and create env
   ```bash
   module purge
   module load anaconda/2023.03-py3.10
   conda create -n pytorch_env python=3.10 -y
   conda activate pytorch_env
   pip install -r requirements.txt
